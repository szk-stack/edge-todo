# Edge Todo nginx 反代示例
# 放到 /etc/nginx/sites-available/edgetodo 后 ln -s 到 sites-enabled
# HTTPS 用 certbot 一键搞定：certbot --nginx -d todo.example.com

server {
    listen 80;
    server_name todo.example.com;  # 改成你的域名

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;

        # WebSocket（/ws 端点）必需的升级头
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WS 长连接保活：覆盖 nginx 默认 60s 读超时
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
